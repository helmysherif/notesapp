function getID(id)
{
    document.getElementById("delete").value = id;
}
function getDetails(title,desc,id)
{
    document.getElementById("title").value = title;
    document.getElementById("desc").value = desc;
    document.getElementById("noteID").value = id;
}